version: "3.9"

services:
  backend:
    build: ./backend
    container_name: stock-intelligence-backend
    env_file:
      - ./backend/.env
    ports:
      - "8000:8000"
    restart: unless-stopped
